---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: machinerequests.butler.butlerlabs.dev
spec:
  group: butler.butlerlabs.dev
  names:
    kind: MachineRequest
    listKind: MachineRequestList
    plural: machinerequests
    singular: machinerequest
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .status.ipAddress
      name: IP
      type: string
    - jsonPath: .spec.role
      name: Role
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: MachineRequest is the Schema for the machinerequests API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: MachineRequestSpec defines the desired state of MachineRequest
            properties:
              cpu:
                description: CPU is the number of CPUs
                format: int32
                minimum: 1
                type: integer
              diskGB:
                description: DiskGB is the disk size in GB
                format: int32
                minimum: 10
                type: integer
              extraDisks:
                description: ExtraDisks defines additional disks to attach
                items:
                  description: MachineRequestDisk defines an extra disk
                  properties:
                    sizeGB:
                      description: SizeGB is the disk size in GB
                      format: int32
                      minimum: 1
                      type: integer
                  required:
                  - sizeGB
                  type: object
                type: array
              labels:
                additionalProperties:
                  type: string
                description: Labels to apply to the machine
                type: object
              machineName:
                description: MachineName is the name for the machine
                type: string
              memoryMB:
                description: MemoryMB is the memory in MB
                format: int32
                minimum: 1024
                type: integer
              providerRef:
                description: ProviderRef references the provider configuration to
                  use
                properties:
                  name:
                    description: Name of the ProviderConfig
                    type: string
                  namespace:
                    description: Namespace of the ProviderConfig
                    type: string
                required:
                - name
                type: object
              role:
                description: Role is the machine role (control-plane or worker)
                enum:
                - control-plane
                - worker
                type: string
              userData:
                description: UserData is the cloud-init or machine configuration
                type: string
            required:
            - cpu
            - diskGB
            - machineName
            - memoryMB
            - providerRef
            - role
            type: object
          status:
            description: MachineRequestStatus defines the observed state of MachineRequest
            properties:
              conditions:
                description: Conditions represent the latest observations
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              failureMessage:
                description: FailureMessage is a human-readable message for the failure
                type: string
              failureReason:
                description: FailureReason is the reason for failure if Phase is Failed
                type: string
              ipAddress:
                description: IPAddress is the primary IP address of the machine
                type: string
              lastUpdated:
                description: LastUpdated is the last time the status was updated
                format: date-time
                type: string
              macAddress:
                description: MACAddress is the MAC address of the machine
                type: string
              phase:
                description: Phase is the current phase of the machine
                type: string
              providerID:
                description: ProviderID is the provider-specific ID for the machine
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
